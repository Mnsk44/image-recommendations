to run the app (+ use dev environment), use these commands:

(windows cmd)
set FLASK_APP=ImageRecs
set FLASK_ENV=development

(other)
export FLASK_APP=ImageRecs
export FLASK_ENV=development

(install application)
pip install -e .

(run application)
flask run

app will run on localhost:5000
